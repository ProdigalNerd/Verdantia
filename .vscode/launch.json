{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: verdantia.app",
            "type": "debugpy",
            "request": "launch",
            "module": "verdantia.app",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
